extends ../base.pug
include ../mixins/video.pug

block content 
    form(method="GET", action="/search")
        input(type="text", placeholder="Search by...", name="keyword", required)
        input(type="submit", value="search")
    each potato in videos 
        +video(potato)
    else 
        span No videos found