extends ../base.pug

block content 
    include ../partials/errorMessage
    form(method="POST")
        input(type="password", placeholder="Current Password", name="current__password", required)
        input(type="password", placeholder="Change Password", name="change__password1", required)
        input(type="password", placeholder="Confirm Password", name="change__password2", required)
        input(type="submit", value="Change Password")