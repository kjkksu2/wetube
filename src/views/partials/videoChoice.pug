div.video__choice.hidden
    if user
        a(href=`/users/${user._id}/upload-video`) 
            i.fab.fa-youtube-square
            span upload video
        a(href="#") 
            i.fas.fa-record-vinyl
            span record video